name: Deploy naar Staging

on:
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: "staging"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Full history + tags needed so "Compute next version number" below can see
          # every existing vN tag, not just what a shallow clone would include.

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Cache node_modules
        id: cache-node-modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: ${{ runner.os }}-node-modules-${{ hashFiles('package-lock.json') }}
        # `npm ci` always wipes node_modules and reinstalls from scratch regardless of
        # what's on disk, so caching the folder only helps if we skip `npm ci` outright
        # on a cache hit (see the `if:` below) -- exact lockfile match only, no
        # restore-keys fallback, since a partial/stale node_modules is exactly what
        # `npm ci` exists to prevent.

      - name: Install dependencies
        if: steps.cache-node-modules.outputs.cache-hit != 'true'
        run: npm ci

      - name: Cache Next.js build cache
        uses: actions/cache@v4
        with:
          path: .next/cache
          key: ${{ runner.os }}-nextjs-staging-${{ hashFiles('package-lock.json') }}-${{ hashFiles('src/**/*.ts', 'src/**/*.tsx', 'src/**/*.js', 'src/**/*.jsx') }}
          restore-keys: |
            ${{ runner.os }}-nextjs-staging-${{ hashFiles('package-lock.json') }}-
        # .next/cache holds Next.js' own incremental compiler cache -- restoring it lets
        # `npm run build` below skip recompiling files that haven't changed since the
        # last run instead of doing a fully cold build every time. Deliberately separate
        # from the deploy payload's .next/cache, which "Assemble deploy payload" further
        # down still strips before upload -- that's about not shipping build-cache files
        # to the server, unrelated to speeding up the build here.
        # Key carries a "staging" tag so this never shares an entry with production's
        # build cache below: pageAvailability.ts reads MIJNHOST_BUILD at build time
        # (src/config/pageAvailability.ts), so the two workflows compile genuinely
        # different output from the same source tree, and cross-restoring one into the
        # other's build risks serving a stale/wrong compiled chunk for the gated routes.

      - name: Controleer databasemigraties
        run: |
          if [ ! -f scripts/check-migrations.ts ]; then
            echo "::warning::scripts/check-migrations.ts ontbreekt in deze versie -- de migratiecontrole is overgeslagen. Dit hoort alleen te gebeuren bij het terugrollen naar een tag van vóór deze controle."
            exit 0
          fi
          npx tsx scripts/check-migrations.ts "https://staging.glassartanddesign.com" staging
        # Faalt vóór de build en vóór de upload als de staging-database een migratie mist
        # die in deze commit zit. De controle gaat via de draaiende app en niet
        # rechtstreeks naar MySQL, omdat de MySQL-grants IP-gebonden zijn en een
        # GitHub-runner er nooit bij kan -- zie het ontwerp in
        # docs/superpowers/specs/2026-08-08-schema-drift-guard-design.md.

      - name: Compute next version number
        id: version
        run: |
          if [ "${{ github.ref }}" != "refs/heads/master" ]; then
            echo "tag=" >> "$GITHUB_OUTPUT"
            echo "Not master -- feature-branch staging deploys aren't versioned (no promotable vN, no beheer version label)."
            exit 0
          fi
          latest=$(git tag -l 'v[0-9]*' | sed 's/^v//' | sort -n | tail -1)
          if [ -z "$latest" ]; then
            next=1
          else
            next=$((latest + 1))
          fi
          echo "tag=v$next" >> "$GITHUB_OUTPUT"
          echo "Next version: v$next"
        # Only computes the candidate number -- the actual git tag is created and pushed
        # in "Tag staged version" below, and ONLY after the smoke check passes. A failed
        # build/deploy/smoke-check simply skips that number; gaps are expected/harmless.
        # Non-master runs deliberately get an empty tag: NEXT_PUBLIC_APP_VERSION ends up
        # unset, so AppVersionLabel shows nothing (same as local dev) instead of a version
        # number that could collide with -- or be confused for -- a real promotable build.

      - name: Build (server mode)
        run: npm run build
        env:
          NEXT_PUBLIC_ENVIRONMENT_LABEL: 'staging'
          NEXT_PUBLIC_APP_VERSION: ${{ steps.version.outputs.tag }}
        # De mail-/uploadwaarden staan hier BEWUST niet meer bij. Next.js bakt elke
        # NEXT_PUBLIC_*-waarde letterlijk in de client-bundle, dus zolang ze hier stonden
        # was het gedeelde relay-secret voor elke bezoeker uit de JavaScript te plukken --
        # en send-mail.php accepteert met dat secret een willekeurige ontvanger. Ze gaan nu
        # als runtime-env mee (zie "Runtime-env uploaden" hieronder), waar de browser er
        # niet bij kan. Niets in de client leest ze nog; alleen /api/mail en /api/upload.
        # No MIJNHOST_BUILD here: staging stays fully open (no Under Construction gate),
        # unlike production. DB_HOST/PORT/USER/PASSWORD/NAME are runtime-only (read by
        # getPool() at request time, not baked in at build time) and are already set
        # directly in the Node.js app's DirectAdmin environment-variables panel --
        # this workflow doesn't need to touch them.

      - name: Assemble deploy payload
        run: |
          rm -rf deploy_payload
          mkdir deploy_payload
          cp -r .next public messages src deploy_payload/
          rm -rf deploy_payload/.next/cache
          cp app.js next.config.mjs package.json package-lock.json deploy_payload/
        # node_modules deliberately excluded -- on this host it's a symlink into a
        # DirectAdmin-managed nodeenv location (visible as `Lrwxrwxrwx` in File Manager),
        # not a plain directory. Trying to upload a real node_modules folder over it fails
        # ("path canonicalization failed"). Managing it is DirectAdmin's "Run NPM Install"
        # button's job, not this workflow's -- see the npm install reminder step below.

      - name: Wait for host reachability
        run: |
          for i in $(seq 1 6); do
            if timeout 5 bash -c "echo > /dev/tcp/h64.mijn.host/22" 2>/dev/null; then
              echo "h64.mijn.host:22 reachable"
              exit 0
            fi
            echo "Attempt $i/6: h64.mijn.host:22 not reachable yet, waiting 10s..."
            sleep 10
          done
          echo "::warning::h64.mijn.host:22 still unreachable after 6 attempts (1 minute) -- proceeding anyway, the SFTP step's own ConnectionAttempts retry may still recover."
        # A one-off "Network unreachable" from the GitHub Actions runner (not this host)
        # has hit both deploy workflows repeatedly -- sftpArgs' ConnectionAttempts=5 below
        # only retries within a single already-started ssh invocation, which doesn't help
        # if the runner has no route at all yet. This waits up to a minute for basic TCP
        # reachability before even attempting the real upload, so a runner-side network
        # blip resolves on its own instead of failing the whole run. Non-fatal by design:
        # if it's still unreachable after a minute, the actual upload step gets a chance
        # to try anyway rather than failing the job here on a possibly-too-strict check.

      - name: Upload build to staging Node.js app via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.6
        with:
          server: ${{ vars.STAGING_FTP_HOST }}
          port: 22
          username: ${{ secrets.STAGING_FTP_USERNAME }}
          password: ${{ secrets.STAGING_FTP_PASSWORD }}
          sftp_only: true
          # Plain FTP/FTPS to this host fails after tens of minutes with "425 Unable to
          # build data connection" -- reproduced identically from a local session and
          # from GitHub Actions, so it's a server-side passive-mode/data-channel problem,
          # not fixable client-side. SFTP tunnels everything over the one already-open
          # connection (confirmed listening: `h64.mijn.host:22` answers with
          # "SSH-2.0-mod_sftp") and has no separate data-channel step to fail.
          #
          # The Node.js app's real "Application root" (confirmed directly in DirectAdmin's
          # Node.js app screen) is a standalone top-level folder, staging.glassartanddesign.
          # com/, a SIBLING of domains/ itself -- not nested inside domains/ at all. Earlier
          # attempts pointed at domains/staging.glassartanddesign.com/glassartanddesign.com/,
          # a real but unrelated, never-executed folder that kept accumulating extra
          # nesting from path experiments. The staging-deploy FTP account's custom
          # directory is now set directly to the true top-level folder, so remote_path
          # here is just "/".
          #
          # local_path uses "*" (not ".*") deliberately: this only matches non-hidden
          # entries (messages/public/src), which is fine here because .next is uploaded
          # separately below -- shell globbing here doesn't expand dotfiles/dotdirs, so a
          # bare "*" would otherwise silently skip .next with no error.
          local_path: './deploy_payload/*'
          remote_path: '/'
          # Retries the underlying sftp client's own connection attempt a few times
          # before giving up, so a one-off "Network unreachable" blip (seen once already)
          # doesn't fail the whole run and require a manual re-dispatch.
          sftpArgs: '-o ConnectionAttempts=5 -o ConnectTimeout=15'
        # sftp_only skips remote directory auto-creation for remote_path itself (must
        # already exist, which it does), but nested folders inside an uploaded directory
        # are still created via SFTP's own MKDIR, no shell needed -- as long as the
        # top-level target folder already exists (confirmed for messages/public/src from
        # the original manual deploy). No delete_remote_files: this only PUTs files that
        # exist locally, it never removes anything not in deploy_payload/ -- tmp/ and
        # stderr.log (Node.js Selector-managed) are left alone by construction.

      - name: Upload .next build via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.6
        with:
          server: ${{ vars.STAGING_FTP_HOST }}
          port: 22
          username: ${{ secrets.STAGING_FTP_USERNAME }}
          password: ${{ secrets.STAGING_FTP_PASSWORD }}
          sftp_only: true
          local_path: './deploy_payload/.next/*'
          remote_path: '/.next'
          sftpArgs: '-o ConnectionAttempts=5 -o ConnectTimeout=15'
        # Separate step because .next is a dotdir the main step's "*" glob can't see (see
        # comment above). remote_path /.next already exists (from the original manual
        # deploy), so this only needs to write/overwrite files inside it, not create it.

      - name: Runtime-env voor de mail-/uploadrelay uploaden
        env:
          SFTP_HOST: ${{ vars.STAGING_FTP_HOST }}
          SFTP_USER: ${{ secrets.STAGING_FTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.STAGING_FTP_PASSWORD }}
          MAIL_ENDPOINT_URL: ${{ vars.NEXT_PUBLIC_MAIL_ENDPOINT_URL }}
          MAIL_SECRET: ${{ secrets.NEXT_PUBLIC_MAIL_SECRET }}
          UPLOAD_ENDPOINT_URL: ${{ vars.NEXT_PUBLIC_UPLOAD_ENDPOINT_URL }}
          UPLOAD_SECRET: ${{ secrets.NEXT_PUBLIC_UPLOAD_SECRET }}
        run: |
          umask 077
          {
            printf 'MAIL_ENDPOINT_URL=%s\n' "$MAIL_ENDPOINT_URL"
            printf 'MAIL_SECRET=%s\n' "$MAIL_SECRET"
            printf 'UPLOAD_ENDPOINT_URL=%s\n' "$UPLOAD_ENDPOINT_URL"
            printf 'UPLOAD_SECRET=%s\n' "$UPLOAD_SECRET"
          } > /tmp/env.production
          sshpass -p "$SFTP_PASSWORD" sftp -o StrictHostKeyChecking=no -o ConnectTimeout=15 -o ConnectionAttempts=5 "$SFTP_USER@$SFTP_HOST" <<< $'put /tmp/env.production /.env.production'
          rm -f /tmp/env.production
        # Deze vier zijn runtime-waarden: alleen /api/mail en /api/upload lezen ze, en dat
        # gebeurt bij een request, niet tijdens de build. Next laadt .env.production bij het
        # opstarten (loadEnvConfig in next/dist/server/config.js), dus dit bestand in de
        # app-root is genoeg -- zonder dat er iets van in de publieke bundle belandt.
        #
        # De GitHub-secrets heten nog NEXT_PUBLIC_* omdat dat de bestaande namen zijn; hier
        # worden ze onder hun nieuwe, niet-publieke namen weggeschreven. Hernoem je ze ooit
        # in GitHub, pas dan alleen de rechterkant hierboven aan.
        #
        # Losse stap met sshpass in plaats van SFTP-Deploy-Action, om dezelfde reden als bij
        # .next hierboven: de "*"-glob van de hoofdupload ziet geen dotfiles. Lokaal heet het
        # bestand daarom env.production en pas op de server .env.production.
        #
        # Staat vóór de restart hieronder, zodat de nieuwe waarden meteen actief zijn.
        # umask 077 houdt het bestand op de runner leesbaar voor alleen deze job; GitHub
        # maskeert de secretwaarden sowieso in de logs.

      - name: Trigger Passenger restart (tmp/restart.txt)
        env:
          SFTP_HOST: ${{ vars.STAGING_FTP_HOST }}
          SFTP_USER: ${{ secrets.STAGING_FTP_USERNAME }}
          SFTP_PASSWORD: ${{ secrets.STAGING_FTP_PASSWORD }}
        run: |
          : > /tmp/restart.txt
          sshpass -p "$SFTP_PASSWORD" sftp -o StrictHostKeyChecking=no -o ConnectTimeout=15 -o ConnectionAttempts=5 "$SFTP_USER@$SFTP_HOST" <<< $'put /tmp/restart.txt /tmp/restart.txt'
          curl -s -o /dev/null "https://staging.glassartanddesign.com/nl" || true
        # Phusion Passenger (this host's Node.js runtime) restarts the app the next time a
        # request reaches it after tmp/restart.txt's mtime changes -- confirmed working
        # 2026-08-01 once pointed at the real Application root (an earlier "doesn't work"
        # finding was actually a misdirected-account bug, not a real Passenger limitation
        # -- see project memory). Uses sshpass + the system sftp client (same underlying
        # tool the SFTP upload steps above already rely on via SFTP-Deploy-Action) rather
        # than curl's sftp:// handler -- curl failed here with exit 60 (SSH host key not
        # trusted) on the GitHub Actions runner even though the identical command works
        # fine locally, so this sidesteps that instead of debugging curl's SSH backend.
        # The local /tmp/restart.txt is a fresh empty file; only the remote mtime bump
        # matters, content is irrelevant. The warm-up curl right after is what makes the
        # restart happen now instead of waiting for the next real visitor.

      - name: npm install reminder
        run: |
          echo '::notice::If package.json/package-lock.json changed in this deploy, DirectAdmin still needs a manual "Run NPM Install" click for staging -- node_modules is a symlink into a DirectAdmin-managed location this workflow cannot upload to. The restart above is automatic either way.'

      - name: Smoke check
        env:
          VERSION_TAG: ${{ steps.version.outputs.tag }}
        run: |
          sleep 5
          attempt=1
          max_attempts=3
          status=""
          while [ "$attempt" -le "$max_attempts" ]; do
            status=$(curl -s -o /dev/null -w "%{http_code}" "https://staging.glassartanddesign.com/nl")
            if [ "$status" = "200" ]; then
              break
            fi
            echo "Attempt $attempt/$max_attempts: staging did not respond with 200 (got $status)"
            attempt=$((attempt + 1))
            if [ "$attempt" -le "$max_attempts" ]; then
              sleep 5
            fi
          done
          if [ "$status" != "200" ]; then
            echo "Staging did not respond with 200 after $max_attempts attempts (last status: $status)"
            exit 1
          fi

          # 20 x 15s = 5 minuten. Ruimer dan de 10 x 10s die het hiervoor was:
          # een trage Passenger-restart haalde die 100 seconden niet, waardoor
          # een geslaagde deploy toch rood werd en -- erger -- de versietag
          # oversloeg, wat promoveren naar productie blokkeert.
          if [ -n "$VERSION_TAG" ]; then
            attempt=1
            max_attempts=20
            found="false"
            while [ "$attempt" -le "$max_attempts" ]; do
              beheer_body=$(curl -s "https://staging.glassartanddesign.com/nl/beheer")
              if echo "$beheer_body" | grep -q ">${VERSION_TAG}<"; then
                found="true"
                break
              fi
              echo "Attempt $attempt/$max_attempts: /nl/beheer does not show $VERSION_TAG yet"
              attempt=$((attempt + 1))
              if [ "$attempt" -le "$max_attempts" ]; then
                sleep 15
              fi
            done
            if [ "$found" != "true" ]; then
              echo "::error::Beheer-header toont niet '$VERSION_TAG' op /nl/beheer na $max_attempts pogingen -- de nieuwe build lijkt niet live te staan."
              exit 1
            fi
            echo "Beheer version label confirmed: $VERSION_TAG"
          fi
        # AppVersionLabel (src/components/AppVersionLabel.tsx) renders NEXT_PUBLIC_APP_VERSION
        # as plain server-rendered text (">v5<" etc, confirmed present in the raw HTML, not
        # just after client-side hydration) -- checking for it here on /nl/beheer catches a
        # deploy that returns 200 but is still serving a stale build (e.g. Passenger not
        # actually restarted), which the plain status-code check above can't distinguish.
        # VERSION_TAG is empty on non-master dispatches (see "Compute next version number"),
        # where AppVersionLabel renders nothing at all -- skip the check entirely then rather
        # than asserting an empty string is present. Retries with its own loop (confirmed
        # needed 2026-08-02): the status-code loop above can pass on its first attempt against
        # the still-running OLD process (Passenger only swaps in the new one asynchronously
        # after the warm-up curl in "Trigger Passenger restart" reaches it), so a single
        # immediate check here can catch that stale process instead of the new build.

      - name: Tag staged version
        if: github.ref == 'refs/heads/master'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "${{ steps.version.outputs.tag }}" "${{ github.sha }}"
          git push origin "${{ steps.version.outputs.tag }}"
          echo "Tagged and pushed ${{ steps.version.outputs.tag }} -- production can now promote this exact commit."
        # Master-only: staging itself can be dispatched from any branch for review, but
        # only a master commit is allowed to become a promotable vN version -- this is
        # what makes production's own master-ref guard meaningful instead of trivially
        # true. Also placed after the smoke check, so this only runs once build, deploy,
        # and smoke check have all succeeded -- but that means the tag says "built and
        # uploaded successfully", not "a human reviewed this version on staging". Actually
        # verifying the new version (after the manual RESTART above) is still the
        # developer's job before ever promoting it to production.
